#include<bits/stdc++.h>

using namespace std;

#define int                      long long
#define for(i, a, n)             for (int i = a; i < n; i++)
#define arrayinput(a, n)         for(i, 0, n) cin >> a[i];
#define arrayprint(a, n)         for (i , 0, n) {cout << a[i] << ' ';} cout << endl;
#define arraysort(a, n)          sort(a, a+n)
#define all(a)                   a.begin(), a.end()

void testcase(){

}

signed main(){
    int t; cin >> t; while (t--)
    {
        testcase();
    }
}